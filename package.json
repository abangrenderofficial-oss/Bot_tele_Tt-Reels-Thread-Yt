{
  "name": "telegram-social-downloader-bot",
  "version": "1.2.1",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "postinstall": "node scripts/install-ytdlp.mjs",
    "check": "node --check api/telegram.js && node --check api/health.js && node --check src/downloader.js && node --check src/telegram.js"
  }
}
